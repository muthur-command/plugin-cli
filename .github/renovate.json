{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "enabledManagers": ["custom.regex"],
  "customManagers": [
    {
      "customType": "regex",
      "fileMatch": ["^Dockerfile$"],
      "matchStrings": [
        "ARG BUILD_FROM=ghcr\\.io/muthur-command/base:(?<currentValue>[^\\s]+)"
      ],
      "datasourceTemplate": "docker",
      "packageNameTemplate": "muthur-command/base",
      "registryUrlTemplate": "https://ghcr.io"
    },
    {
      "customType": "regex",
      "fileMatch": ["^Dockerfile$"],
      "matchStrings": ["ARG CLI_VERSION=(?<currentValue>[^\\s]+)"],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "muthur-command/cli",
      "versioningTemplate": "loose"
    }
  ],
  "packageRules": [
    {
      "matchDatasources": ["docker"],
      "matchPackageNames": ["muthur-command/base"],
      "groupName": "docker-base-alpine",
      "commitMessageTopic": "muthur-command/base image",
      "commitMessageExtra": "to {{newVersion}}",
      "versioning": "loose"
    },
    {
      "matchManagers": ["custom.regex"],
      "matchDepNames": ["muthur-command/cli"],
      "groupName": "cli",
      "extractVersion": "^(?:mc_)?(?<version>.*)$",
      "commitMessageTopic": "cli",
      "commitMessageExtra": "to {{newVersion}}"
    }
  ],
  "schedule": ["every 12 hours"]
}
